import localFont from 'next/font/local'

const recoleta = localFont({ src: './Recoleta-Bold.ttf' })
const modernEra = localFont({ src: './Modern-era-Regular.ttf' })
const guthenBloots = localFont({ src: './Guthen-Bloots-Personal-Use.ttf' })

export { recoleta, modernEra, guthenBloots }